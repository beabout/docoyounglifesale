%h1.bold Categories

%p These are all product categories. Click on one to see all products under that category. 

%hr

.row 
  - @product_types.each do |type|
    .col-6.col-md-4
      = link_to type.name, product_type_path(type), class: 'btn btn-primary category-btn'

.text-center
  %a.btn.btn-primary.sale-btn{href: root_path} Back to homepage 